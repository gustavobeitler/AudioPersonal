name: Build Safe Audio Core

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Compilación temporalmente en pausa durante la actualización del núcleo"
